apiVersion: apps/v1
kind: Deployment
metadata:
  name: mywebapp-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mywebapp
  template:
    metadata:
      labels:
        app: mywebapp
    spec:
      containers:
      - name: mywebapp
        image: blackberry07/mywebapp:latest  # your Docker Hub image
        
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
